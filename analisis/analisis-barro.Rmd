---
title: "barro-myth"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
```

```{r}
data <- read_csv("../scrapeo/personajes.csv")

names(data)[3:141]

data %>%
  filter(Nombre == "Colo Colo")

data %>%
  filter(`Mitología española` == 1) %>%
  select(Nombre, `Mitología íbera`, `Mitología española`)

```

